# Lokale Codex-Konfiguration für diese Projektvorlage.
#
# Diese Datei hält nur Einstellungen, die Codex beim Start helfen. Fachliche
# Regeln bleiben in AGENTS.md, GRUNDREGELN.md und AI/PROJEKTREGELN/.

project_doc_max_bytes = 65536
project_doc_fallback_filenames = ["CLAUDE.md", "readmefirst.md"]

[features]
hooks = true
