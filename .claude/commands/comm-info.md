# /comm-info

Zeige eine Liste der Claude-Codex-MCP-Befehle mit kurzer Erklärung.

Nutze nach Möglichkeit:

```bash
npm --silent --prefix PROJEKT/WORKSPACE run comm -- info
```

Erkläre mindestens `/comm`, `/comm-help`, `/comm-info`, `/comm-clear` und
`/comm-clear-backup`.
