export function renderCliHelp(): string {
  return [
    "Claude-Codex-MCP: einfacher comm-Befehl",
    "",
    "Nutzung lokal aus dem Repository:",
    "",
    "  npm --silent --prefix PROJEKT/WORKSPACE run comm -- setup",
    "  npm --silent --prefix PROJEKT/WORKSPACE run comm -- doctor --project-dir ./mein-projekt",
    "  npm --silent --prefix PROJEKT/WORKSPACE run comm -- status --project-dir ./mein-projekt",
    "  npm --silent --prefix PROJEKT/WORKSPACE run comm -- start --project-dir ./mein-projekt",
    "",
    "Nutzung nach einer spaeteren NPM-Installation:",
    "",
    "  claude-codex-mcp setup",
    "  claude-codex-mcp doctor --project-dir ./mein-projekt",
    "  claude-codex-mcp status --project-dir ./mein-projekt",
    "  claude-codex-mcp start --project-dir ./mein-projekt",
    "",
    "Befehle:",
    "",
    "  setup   Richtet agent_comms.md, agent_comms.state.json und Hilfe-Dateien ein.",
    "  doctor  Prüft verständlich, ob Node.js und die Agenten-Dateien vorhanden sind.",
    "  status  Zeigt offene Aufgaben, Blocker und letzte Aktivität.",
    "  start   Startet den lokalen MCP-Server über stdio für Codex oder Claude.",
    "  help    Zeigt diese Hilfe.",
    "",
    "Wichtig:",
    "",
    "  Der MCP läuft lokal. Schreibe keine Passwörter, Tokens, .env-Inhalte",
    "  oder personenbezogenen Details in Aufgaben, Chats oder Logs.",
    "",
    "Hinweis:",
    "",
    "  Auf macOS gibt es bereits einen Systembefehl namens `comm`.",
    "  Wenn im Terminal `usage: comm [-123i] file1 file2` erscheint,",
    "  wurde der macOS-Systembefehl gestartet, nicht Claude-Codex-MCP.",
    "",
    "  Nutze lokal aus dem Repository diesen vollständigen Befehl:",
    "",
    "  npm --silent --prefix PROJEKT/WORKSPACE run comm -- help",
    ""
  ].join("\n");
}
